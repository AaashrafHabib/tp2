import { ArcEncielDirective } from './arc-enciel.directive';

describe('ArcEncielDirective', () => {
  it('should create an instance', () => {
    const directive = new ArcEncielDirective();
    expect(directive).toBeTruthy();
  });
});
